@{
    ViewBag.Title = "栏目管理";
    Layout = "~/Views/Shared/_TreeList.cshtml";

    ViewBag.Modules = "Column";
    ViewBag.Ext = "List";
}
@section HeadContent {
    <script>
        var listObj = null;

        function updateNode(type, data) {
            if (listObj) {
                listObj.updateNode(type, data);
            }
        }
        
    </script>
}
